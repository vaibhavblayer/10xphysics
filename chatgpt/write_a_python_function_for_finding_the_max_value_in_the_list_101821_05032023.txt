Q. Write a python function for finding the max value in the list.

def find_max(list):
  max_value = list[0]
  for i in list:
    if i > max_value:
      max_value = i
  return max_value

list = [1,2,3,4,5]

print(find_max(list)) # Output: 5