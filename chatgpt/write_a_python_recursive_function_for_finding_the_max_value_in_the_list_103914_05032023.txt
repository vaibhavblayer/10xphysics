Q. Write a python recursive function for finding the max value in the list.

def find_max(list): 
    if len(list) == 1: 
        return list[0] 
    else: 
        m = find_max(list[1:]) 
        return m if m > list[0] else list[0] 
  
list = [1, 2, 3, 4, 5] 
print(find_max(list))